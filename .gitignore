node_modules/
data/
logs/
.env
.env.md
.DS_Store
*.db

# Ignore all of dist/ except the pre-built bundle
# Note: sql-wasm.wasm is now embedded inside bundle.js (esbuild --loader:.wasm=binary)
# so no separate WASM file is needed in dist/
dist/*
!dist/bundle.js
